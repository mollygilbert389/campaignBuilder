        // //for creating name
        // let newDiv = document.createElement('div')
        // let newLabel = document.createElement('label')
        // let newInput = document.createElement('input')
        // let newForm = document.createElement('form')
        // let newBtn = document.createElement('button')
        // const partySpace = document.getElementById('partySpace')
        // //for choosing type
        // let typeDiv = document.createElement('div')
        // let typeLabel = document.createElement('label')
        // let typeSelect = document.createElement('select')
        // let typeOption = document.createElement('option')

        // // //for loop?

        // // //creates 1st player
        // partySpace.append(newDiv)
        // newDiv.append(newForm)
        // newForm.append(newLabel)
        // newLabel.append("Player #1 Name: ")
        // newForm.append(newInput)
        // newForm.append(newBtn)
        // newBtn.append("Save")

        // // //creates the Class list
        // newDiv.append(typeDiv)
        // typeDiv.append(typeLabel)
        // typeDiv.append(typeSelect)
        // typeSelect.append(typeOption)
        
        // const addOptions = playerClass.map((class) =>
        // // typeSelect.append(typeOption.class)
        // console.log(class)
        // );


        //look through the array
        //push array vaules into the options
        //append the options to the div 

        //create 2nd player
        // partySpace.append(newDiv)
        // newDiv.append(newForm)
        // newForm.append(newLabel)
        // newLabel.append("Player #2 Name: ")
        // newForm.append(newInput)